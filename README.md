# KillKeyboard
C++ code to kill on screen keyboard(windows 10 tablet mode), since it hang often.

Compile on Visual Studio 2017 community

cmd> cl /EHsc /O2 killkeyboard.cpp

copy exe into somewhere (e.g., "C:\User\%username%\" )
right click and pin to start
